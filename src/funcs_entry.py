def prompt_init(dict_cur: dict):
    print(f'\nX characters currently tracked ({len(dict_cur)} in database)\n'
          'Please select an option:\n'
          '\t1 - Add character to database\n'
          '\t2 - Save and exit\n'
          '\t3 - Exit without saving')
